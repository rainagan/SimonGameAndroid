<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="rainagan.simongamendroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".welcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chooseDiffActivity">
        </activity>
        <activity android:name=".chooseButtonActivity">
        </activity>
        <activity android:name=".oneButtonActivity">
        </activity>
        <activity android:name=".twoButtonActivity">
        </activity>
        <activity android:name=".threeButtonActivity">
        </activity>
        <activity android:name=".fourButtonActivity">
        </activity>
        <activity android:name=".fiveButtonActivity">
        </activity>
        <activity android:name=".sixButtonActivity">
        </activity>
    </application>

</manifest>